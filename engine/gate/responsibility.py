"""Responsibility gate skeleton for docs/greenlight/05 §2.4 and 06 Phase 1."""

from schemas.models import GateResult, ValidatedProfile


def evaluate_gate(profile: ValidatedProfile) -> GateResult:
    """Evaluate the responsibility gate per docs/greenlight/05 §2.4."""

    raise NotImplementedError
