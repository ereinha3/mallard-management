node_modules/
dist/
.next/
.DS_Store

# Env files — never commit secrets
.env.local
**/.env

# Build artifacts
tsconfig.tsbuildinfo
*.tsbuildinfo

# ETF universe cache — rebuilt by scripts/buildEtfUniverse.ts
data/etf-universe.json

# Client portfolio snapshots and optimization reports — NEVER commit client data
data/clients/
data/optimization-reports/

# Historical market data — too large for git, share directly with Ethan
data/market-data/

# Misc
**/.codex-tasks
client-integration/
**/.cursor*
**/.playwright*
**/*.db

# added by stack init
.env
.mcp.json

*.mcp-scope.yaml
CLAUDE.md
STATUS.md
opencode.json
.mcp.json
