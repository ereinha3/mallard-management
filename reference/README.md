# TypeScript Reference

Gilbert's TypeScript tax / location (state tax by ZIP) / waterfall (asset-location) / ETF-universe + market-data tooling.

**More complete than the Python engine's current tax stub.**

NOT wired into the running app - preserved to be ported into the Python `engine/` (roadmap P3).

To run its tests:

```sh
cd reference
npm install
npm test
```

The running app is the Python `backend/` + `engine/` with the Vite `client/`. This directory is self-contained reference material only.
